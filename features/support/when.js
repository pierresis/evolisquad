const { Given, When, Then } = require('cucumber');

When('the User start the simulation', function () {
    // Write code here that turns the phrase above into concrete actions
});

When('a User start a simulation', function () {
    // Write code here that turns the phrase above into concrete actions
});