const { Given, When, Then } = require('cucumber');
const { expect } = require('chai');

Given('a population with an initial count of {int} Evoli', function (initialPopulationCount) {
    // Write code here that turns the phrase above into world changes
});

Given('a map full of grass', function () {
    // Write code here that turns the phrase above into world changes
});

Given('a map of size {int} per {int} with a biome {string}', function (length, width, biomeType) {
    // Write code here that turns the phrase above into world changes
});